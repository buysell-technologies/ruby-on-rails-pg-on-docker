<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <%# taskはshowアクションへのリンクに変換してくれる %>
    <li><%= link_to task.id, task %> : <%= task.name %></li>
  <% end %>
</ul>

<%= link_to 'タスクの作成', new_task_path %>